<Window x:Class="CooridnateGrid.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CooridnateGrid"
        xmlns:vect="clr-namespace:System.Numerics;assembly=System.Numerics"
        xmlns:drawObj="clr-namespace:CooridnateGrid.DrawingObjects"
        xmlns:coordPlane="clr-namespace:CooridnateGrid.CoordinatePlane"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Title="MainWindow">
    <Window.Resources>
        <drawObj:Vector2ToStringConverter x:Key="Vector2Converter"/>
        <drawObj:Circle x:Key="mainCircle"/>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="10"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="10"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="10"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Column="1" Grid.Row="1" MinWidth="200" 
                    Background="LightBlue" Margin="0 0 10 0" Orientation="Vertical"
                    DataContext="{DynamicResource mainCircle}">
            <TextBlock Text="Radius" FontSize="20" Margin="20" TextAlignment="Center"></TextBlock>
            <TextBox x:Name="RSetField" MinWidth="20" 
               Text="{Binding R, Converter={StaticResource Vector2Converter}, UpdateSourceTrigger = PropertyChanged}"></TextBox>
        </StackPanel>
        <Border BorderBrush="Green" BorderThickness="4" Grid.Row="1" Grid.Column="2">
            <Grid x:Name="ViewPortContainer">

                <Image x:Name="ViewPort" Loaded="ViewPort_Loaded"></Image>
            </Grid>
        </Border>
    </Grid>
</Window>
